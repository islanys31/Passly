server {
    listen 80;
    server_name localhost;

    # Optimización de compresión Nginx
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_proxied any;
    gzip_min_length 1000;

    # Redirección automática a HTTPS (comentada hasta tener certificados)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://api:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Configuración sugerida para HTTPS (una vez configurado Certbot/SSL)
# server {
#     listen 443 ssl http2;
#     server_name localhost;
#
#     ssl_certificate /etc/nginx/ssl/live/passly.com/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/live/passly.com/privkey.pem;
#
#     location / {
#         proxy_pass http://api:3000;
#         ...
#     }
# }
